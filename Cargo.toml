[package]
name = "nav-cli"
version = "0.4.0"
edition = "2021"
description = "A fast, tag-based terminal bookmark manager for jumping between projects."
license = "MIT"
repository = "https://github.com/15935648a/nav"
readme = "README.md"

[[bin]]
name = "nav"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
